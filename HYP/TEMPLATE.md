# HYP-000 — one-line title

**Status:** proposed | building | reviewed | merged | killed
**Date:** YYYY-MM-DD  **Pod:** moon | surge | gapgo | reclaim | flag | igniter | redgreen | (discovery)

## Claim
One sentence. Specific and numeric.

## Category
(a) discovery gate in `lib/data.js` · (b) entry signal in `lib/strategies.js` ·
(c) parameter range in `lib/strategy.js` — name the file and the pod.

## Mechanism
Two sentences. Not a story about one ticker.

## Falsifier
The exact backtest result that kills this. Written BEFORE any numbers exist.

## Cost
How many of yesterday's ranked movers does this add or remove?

---

## Result  (builder fills in — two seeds, never one)

| | champion | this (seed 11) | this (seed 29) |
|---|---|---|---|
| trades | | | |
| netPct | | | |
| maxDDPct | | | |
| train score | | | |
| validate score | | | |

```
# commands run
```

## Verdict
KILL / SURVIVES-REVIEW — one sentence, link `REVIEWS/HYP-000.md`.
